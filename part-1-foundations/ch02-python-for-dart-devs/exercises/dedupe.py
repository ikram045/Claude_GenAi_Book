"""Exercise 1 — Remove duplicates, preserve order.

The naive solution is O(n²) and the test will time out on 100,000 items.
Think about which data structure makes a membership test O(1).
"""


def dedupe(items: list[str]) -> list[str]:
    """Return items with duplicates removed, in first-seen order.

    >>> dedupe(["a", "b", "a", "c", "b"])
    ['a', 'b', 'c']
    """
    raise NotImplementedError("your turn")
